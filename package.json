{
  "name": "01-worktop",
  "version": "0.0.0",
  "devDependencies": {
    "worktop.build": "^0.0.5",
    "wrangler": "3.9.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "build": "worktop build src/index.js",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "faunadb": "^4.8.0",
    "worktop": "^0.7.3"
  }
}
